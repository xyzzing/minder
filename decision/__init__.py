"""Decision contracts (docs/minder-phases-4-7-with-5.5-6.5.md Phase 5.5).

The Foreman-style assess loop: compact state, closed menu, typed
questions, Python gate. The model (System One / gateway) PROPOSES from a
closed menu; deterministic policy DECIDES; tests and fresh tool results
PROVE. Default providers are Null/Fake — no Jev/Laya key, no GPU, and no
network anywhere in the default test run.
"""
